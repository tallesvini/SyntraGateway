namespace Syntra.SharedKernel.Results
{
    public enum ResultErrorType
    {
        None,
        Validation,
        NotFound,
        Conflit,
        Unauthorized,
        Forbidden,
        Failure
    }
}
